---
- name: privilage escalation example
  hosts: all
  tasks:
    - name: install httpd
      yum:
        name: httpd
        state: latest
      when: ansible_os_family: "RedHat"
      become: True
    - name: Insstall apache2
      apt:
        name: apache2
        state: latest
      when: ansible_os_family: "Debian"