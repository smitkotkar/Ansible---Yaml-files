- name: install httpd
  package:
    name: "{{PKG}}"
    state: latest
- name: deploy application
  copy:
    src: index.html
    dest: /var/www/html/index.html
- name: start service
  systemd:
    name: "{{PKG}}"
    state: started
    enabled: True