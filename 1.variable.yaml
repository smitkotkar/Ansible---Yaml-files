---
- name: variable example
  hosts: all
  vars:
     URL: "smitkotkar.com"
  tasks:
    - name: print URL
      debug:
        msg: "{{URL}}"
    - name: Print another URL
      debug:
        msg: "{{URL}}"
...
